 <h2 id="welcome" class="page-header">Welcome, {{user.name}}</h2>
 <p class="instruct">please select the character in which you'd like to play</p>

{{!-- {{#each character}}
  <button id="character" type="submit">{{character.name}}</button>
{{/each}}
 --}}


<form class="formBtn" action="/createCharacter">
 <button>Create a new Character</button>
</form>

<form class="formBtn"  action="/characters">
 <button>Select Existing Character</button>
</form>

<form class="formBtn"  action="/dm">
 <button>Play as Dungeon Master</button>
</form>